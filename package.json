{
    "name": "secret-share",
    "version": "1.0.0",
    "private": true,
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "docker-compose up -d --build --remove-orphans",
        "stop": "docker-compose down -v",
        "logs": "docker-compose logs -f web",
        "debug": "npm restart && npm run logs",
        "postinstall": "npm install -C web",
        "test": "docker-compose run web npm test",
        "test:watch": "docker-compose run web npm run test:watch",
        "view:coverage": "echo 'TODO: web coverage' && exit 1 && docker-compose run -p 5000:5000 web npm run view:coverage"
    },
    "author": "",
    "license": "MIT",
    "dependencies": {}
}
