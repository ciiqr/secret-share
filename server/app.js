// TODO: install koa & others
// TODO: maybe rename this project folder: server => backend
const Koa = require('koa');

const app = new Koa();

// TODO: setup app
// app.use(/* register your middleware as normal */);

module.exports = app;
